# Frontend API URL embedded at build time
VITE_API_BASE_URL=http://localhost:8000

# Preferred hackathon inference path: Fireworks-hosted Gemma on AMD infrastructure
FIREWORKS_API_KEY=
FIREWORKS_MODEL=accounts/fireworks/models/gemma-4-26b-a4b-it

# Optional hash-linked AMD/ROCm artifact inside the backend container
AMD_PROOF_FILE=/app/artifacts/amd-run.json

# Optional Google AI API fallback
GEMMA_API_KEY=
GEMMA_MODEL=gemma-4-26b-a4b-it

# Comma-separated frontend origins allowed by FastAPI
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
