# Verified runtime artifacts

Place the unchanged `amd-run.json` generated by `scripts/amd_capture.py` in this directory only after a real AMD/ROCm run succeeds.

The backend rejects unreadable, modified, incomplete, or failed artifacts. Never place credentials, environment files, provider keys, or unrelated logs here.
